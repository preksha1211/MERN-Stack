    sayHi(); // we can call the function before its definition beacuse in memory creation phase its definition is stored
    var username='anurag';
    var userAge=25;
    
    function sayHi(){
        const a=14;
        const b=12;
        console.log(a,b);;

    }

 